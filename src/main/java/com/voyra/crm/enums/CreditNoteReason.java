package com.voyra.crm.enums;

/** Why a {@code credit_note} was raised against an issued tax invoice. */
public enum CreditNoteReason {
    BOOKING_CANCELLED, SERVICE_DOWNGRADED, BILLING_ERROR, GOODWILL, OTHER
}
