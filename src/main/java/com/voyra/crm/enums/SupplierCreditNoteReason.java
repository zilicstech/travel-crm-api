package com.voyra.crm.enums;

/** Why the supplier issued us a credit note - mirrors {@code CreditNoteReason} for AR. */
public enum SupplierCreditNoteReason {
    BOOKING_CANCELLED, RATE_CORRECTION, BILLING_ERROR, OTHER
}
