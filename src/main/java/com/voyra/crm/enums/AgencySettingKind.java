package com.voyra.crm.enums;

/** serviceType is set on the entity only when kind is SERVICE_PREFERENCE. */
public enum AgencySettingKind {
    LEAD_SOURCE, TRAVEL_CATEGORY, DOCUMENT_TYPE, SERVICE_PREFERENCE
}
